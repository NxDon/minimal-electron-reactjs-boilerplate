import React from 'react';

const PageNotFound = props => {
    return (
        <div>
            404 Not Found
        </div>
    );
};

PageNotFound.propTypes = {
    
};

export default PageNotFound;
